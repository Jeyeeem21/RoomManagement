<!doctype html>
<html lang="en">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <title>{{title}}</title>
    <link rel="stylesheet" href="/tailwind.css" />
    <script src="https://cdn.tailwindcss.com"></script>
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;500;600;700&display=swap" rel="stylesheet" />
    <style>
      /* Color tokens */
      :root{
        --primary:#344158; /* dark blue-gray */
        --accent-green:#28A745;
        --accent-gold:#FFD700;
        --bg-light:#F4F6F8;
      }
      /* Typography */
      html, body { font-family: 'Inter', system-ui, -apple-system, Segoe UI, Roboto, Ubuntu, Cantarell, 'Helvetica Neue', Arial, 'Noto Sans', 'Apple Color Emoji', 'Segoe UI Emoji', 'Segoe UI Symbol'; color:#111827 }
      h1, h2, h3 { letter-spacing: -0.01em }
      h1 { font-weight:700 }
      h2 { font-weight:600 }
      h3 { font-weight:600 }
      /* Sidebar collapse transitions */
      .sidebar-transition{transition:width .25s ease, padding .25s ease}
      .sidebar-collapsed .sidebar-text{display:none}
      .sidebar-collapsed .sidebar{width:72px}
      .sidebar-collapsed .sidebar .logo-text{display:none}
      .sidebar-collapsed .sidebar .icon-only{display:block}
      .sidebar .icon-only{display:none}
      /* Sidebar styling helpers */
      .sidebar a{font-size:.95rem; border-radius:.5rem; transition:background-color .15s ease, color .15s ease}
      .sidebar a .sidebar-text{color:#111827}
      .sidebar a svg{color:#111827}
      .sidebar a:focus-visible{outline:2px solid var(--accent-green); outline-offset:2px; border-radius:.5rem}
      .active-link{background-color:var(--accent-green); box-shadow:inset 3px 0 0 0 rgba(0,0,0,.15)}
      .active-link .sidebar-text{color:#fff; font-weight:600}
      .active-link svg{color:#fff}
      .card-border{border-left:4px solid var(--accent-green)}
    </style>
  </head>
  <body>
    <div id="app-shell" class="flex min-h-screen">